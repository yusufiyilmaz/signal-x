<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Signal-X | Ag Guvenlik Denetcisi</title>
    <style>
        :root {
            --neon: #bf5fff;
            --neon2: #ff2d9b;
            --neon3: #00f0ff;
            --bg: #07050f;
            --bg2: #0e0a1a;
            --bg3: #160d28;
            --border: #2a1550;
            --text: #d0c0f0;
            --dim: #4a3570;
        }
        .theme-matrix {
            --neon: #00ff41;
            --neon2: #00cc33;
            --neon3: #00ff99;
            --bg: #000300;
            --bg2: #001a00;
            --bg3: #002800;
            --border: #003a00;
            --text: #c0ffc0;
            --dim: #1a5a1a;
        }
        .theme-danger {
            --neon: #ff3060;
            --neon2: #ff6000;
            --neon3: #ffcc00;
            --bg: #0f0003;
            --bg2: #1a0005;
            --bg3: #280008;
            --border: #3a000a;
            --text: #ffc0c0;
            --dim: #5a1a1a;
        }
        .theme-cyber {
            --neon: #00cfff;
            --neon2: #0080ff;
            --neon3: #00ffee;
            --bg: #00080f;
            --bg2: #000f1a;
            --bg3: #001528;
            --border: #002040;
            --text: #c0e0ff;
            --dim: #1a3a5a;
        }
        .theme-gold {
            --neon: #ffd700;
            --neon2: #ff8c00;
            --neon3: #fffacd;
            --bg: #0a0800;
            --bg2: #150f00;
            --bg3: #201500;
            --border: #3a2800;
            --text: #fff0c0;
            --dim: #5a4a00;
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { background: var(--bg); color: var(--text); font-family: 'Courier New', monospace; min-height: 100vh; transition: all 0.3s; }
        body::after { content: ''; position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: repeating-linear-gradient(0deg, transparent, transparent 2px, rgba(0,0,0,0.03) 2px, rgba(0,0,0,0.03) 4px); pointer-events: none; z-index: 9999; }

        /* HEADER */
        .header { background: var(--bg2); border-bottom: 1px solid var(--neon); padding: 14px 40px; display: flex; align-items: center; justify-content: space-between; position: sticky; top: 0; z-index: 100; box-shadow: 0 0 30px rgba(0,0,0,0.5); }
        .logo-text { color: var(--neon); font-size: 22px; font-weight: bold; letter-spacing: 4px; text-shadow: 0 0 20px var(--neon); }
        .logo-sub { color: var(--dim); font-size: 10px; margin-top: 3px; letter-spacing: 2px; }
        .header-right { display: flex; align-items: center; gap: 12px; flex-wrap: wrap; }
        .status-pill { display: flex; align-items: center; gap: 8px; background: var(--bg3); border: 1px solid var(--neon); padding: 5px 12px; border-radius: 20px; }
        .status-dot { width: 7px; height: 7px; background: var(--neon); border-radius: 50%; box-shadow: 0 0 8px var(--neon); animation: pulse 2s infinite; }
        .status-text { color: var(--neon); font-size: 11px; letter-spacing: 1px; }
        @keyframes pulse { 0%,100%{opacity:1} 50%{opacity:0.3} }

        /* TEMA BUTONLARI */
        .theme-switcher { display: flex; gap: 6px; align-items: center; }
        .theme-btn { width: 22px; height: 22px; border-radius: 50%; border: 2px solid transparent; cursor: pointer; transition: all 0.2s; }
        .theme-btn:hover { transform: scale(1.2); }
        .theme-btn.active { border-color: white; box-shadow: 0 0 8px white; }
        .theme-btn.t-cyber { background: linear-gradient(135deg, #bf5fff, #ff2d9b); }
        .theme-btn.t-matrix { background: linear-gradient(135deg, #00ff41, #00cc33); }
        .theme-btn.t-danger { background: linear-gradient(135deg, #ff3060, #ff6000); }
        .theme-btn.t-blue { background: linear-gradient(135deg, #00cfff, #0080ff); }
        .theme-btn.t-gold { background: linear-gradient(135deg, #ffd700, #ff8c00); }
        .theme-label { color: var(--dim); font-size: 10px; letter-spacing: 1px; }

        /* CONTAINER */
        .container { max-width: 1300px; margin: 0 auto; padding: 30px 20px; }
        .page-title { font-size: 12px; color: var(--dim); letter-spacing: 3px; margin-bottom: 20px; }
        .page-title span { color: var(--neon2); }

        /* TABS */
        .tabs { display: flex; gap: 4px; margin-bottom: 24px; }
        .tab { padding: 9px 22px; border-radius: 6px; cursor: pointer; color: var(--dim); font-size: 12px; letter-spacing: 1px; border: 1px solid var(--border); background: var(--bg2); transition: all 0.2s; }
        .tab:hover { border-color: var(--neon); color: var(--neon); }
        .tab.active { background: var(--bg3); border-color: var(--neon); color: var(--neon); box-shadow: 0 0 15px rgba(0,0,0,0.3); }

        /* CARDS */
        .card { background: var(--bg2); border: 1px solid var(--border); border-radius: 10px; padding: 24px; margin-bottom: 20px; position: relative; overflow: hidden; }
        .card::before { content: ''; position: absolute; top: 0; left: 0; right: 0; height: 1px; background: linear-gradient(90deg, transparent, var(--neon), transparent); }
        .card-title { color: var(--neon); font-size: 13px; margin-bottom: 18px; letter-spacing: 2px; }

        /* FORM */
        .form-row { display: flex; gap: 10px; flex-wrap: wrap; align-items: center; }
        input[type=text], input[type=number] { background: var(--bg); border: 1px solid var(--border); color: var(--text); padding: 10px 14px; border-radius: 6px; font-family: monospace; font-size: 13px; flex: 1; min-width: 140px; transition: all 0.2s; }
        input:focus { outline: none; border-color: var(--neon); box-shadow: 0 0 10px rgba(0,0,0,0.3); }
        input::placeholder { color: var(--dim); }

        /* BUTTONS */
        .btn { padding: 10px 24px; border-radius: 6px; font-weight: bold; cursor: pointer; font-family: monospace; font-size: 12px; border: none; transition: all 0.2s; letter-spacing: 1px; }
        .btn-primary { background: linear-gradient(135deg, var(--neon), var(--neon2)); color: #000; box-shadow: 0 0 20px rgba(0,0,0,0.3); }
        .btn-primary:hover { box-shadow: 0 0 30px rgba(0,0,0,0.5); transform: translateY(-1px); }
        .btn-primary:disabled { background: var(--border); color: var(--dim); box-shadow: none; cursor: not-allowed; transform: none; }
        .btn-secondary { background: transparent; color: var(--neon); border: 1px solid var(--neon); }
        .btn-secondary:hover { background: rgba(0,0,0,0.2); }
        .btn-cyan { background: transparent; color: var(--neon3); border: 1px solid var(--neon3); }
        .btn-cyan:hover { background: rgba(0,0,0,0.2); }

        /* PROGRESS */
        .progress-wrap { margin-top: 16px; display: none; }
        .progress-bar { background: var(--bg3); border-radius: 4px; height: 4px; overflow: hidden; }
        .progress-fill { height: 100%; background: linear-gradient(90deg, var(--neon), var(--neon2), var(--neon3), var(--neon)); background-size: 300% 100%; border-radius: 4px; animation: progressAnim 2s linear infinite; }
        @keyframes progressAnim { 0%{background-position:0%} 100%{background-position:300%} }
        .progress-text { color: var(--dim); font-size: 11px; margin-top: 8px; letter-spacing: 1px; }

        /* STATS */
        .stats-row { display: grid; grid-template-columns: repeat(auto-fit, minmax(160px, 1fr)); gap: 14px; margin-bottom: 20px; }
        .stat-card { background: var(--bg2); border: 1px solid var(--border); border-radius: 10px; padding: 18px 20px; position: relative; overflow: hidden; transition: all 0.2s; }
        .stat-card:hover { border-color: var(--neon); }
        .stat-card::after { content: ''; position: absolute; bottom: 0; left: 0; right: 0; height: 2px; background: linear-gradient(90deg, var(--neon), var(--neon2)); }
        .stat-label { color: var(--dim); font-size: 10px; letter-spacing: 2px; margin-bottom: 10px; }
        .stat-value { font-size: 20px; font-weight: bold; color: var(--neon); text-shadow: 0 0 10px var(--neon); }
        .stat-value.red { color: #ff3060; text-shadow: 0 0 10px #ff3060; }
        .stat-value.neon2 { color: var(--neon2); text-shadow: 0 0 10px var(--neon2); }
        .stat-value.neon3 { color: var(--neon3); text-shadow: 0 0 10px var(--neon3); }

        /* SCORE */
        .score-badge { display: inline-flex; align-items: center; justify-content: center; width: 44px; height: 44px; border-radius: 8px; font-weight: bold; font-size: 22px; }
        .score-A { background: rgba(0,240,255,0.1); color: #00f0ff; border: 1px solid #00f0ff; box-shadow: 0 0 10px rgba(0,240,255,0.3); }
        .score-B { background: rgba(0,255,65,0.1); color: #00ff41; border: 1px solid #00ff41; }
        .score-C { background: rgba(255,200,0,0.1); color: #ffc800; border: 1px solid #ffc800; }
        .score-D { background: rgba(255,100,0,0.1); color: #ff6400; border: 1px solid #ff6400; }
        .score-E, .score-F { background: rgba(255,48,96,0.1); color: #ff3060; border: 1px solid #ff3060; box-shadow: 0 0 10px rgba(255,48,96,0.3); }

        /* TABLE */
        .table-wrap { overflow-x: auto; }
        table { width: 100%; border-collapse: collapse; }
        th { background: var(--bg3); color: var(--neon); padding: 10px 14px; text-align: left; font-size: 10px; letter-spacing: 2px; border-bottom: 1px solid var(--border); }
        td { padding: 10px 14px; border-bottom: 1px solid var(--border); font-size: 13px; }
        tr:hover td { background: rgba(0,0,0,0.2); }
        .badge { padding: 3px 10px; border-radius: 4px; font-size: 10px; font-weight: bold; letter-spacing: 1px; }
        .badge-open { background: rgba(0,240,255,0.1); color: #00f0ff; border: 1px solid #00f0ff; }
        .badge-alive { background: rgba(0,0,0,0.2); color: var(--neon); border: 1px solid var(--neon); }
        .badge-risk { background: rgba(255,48,96,0.1); color: #ff3060; border: 1px solid #ff3060; }
        .badge-warn { background: rgba(255,100,0,0.1); color: #ff6400; border: 1px solid #ff6400; }
        .badge-low { background: rgba(0,240,255,0.1); color: #00f0ff; border: 1px solid #00f0ff; }

        /* REPORT */
        pre { background: var(--bg); border: 1px solid var(--border); padding: 20px; border-radius: 8px; overflow-x: auto; font-size: 12px; line-height: 1.8; color: var(--text); }
        .report-actions { display: flex; gap: 10px; margin-top: 14px; flex-wrap: wrap; }

        .panel { display: none; }
        .panel.active { display: block; }
        .scan-meta { display: flex; gap: 20px; margin-bottom: 16px; flex-wrap: wrap; }
        .scan-meta-item { color: var(--dim); font-size: 11px; letter-spacing: 1px; }
        .scan-meta-item span { color: var(--neon); }

        ::-webkit-scrollbar { width: 6px; height: 6px; }
        ::-webkit-scrollbar-track { background: var(--bg); }
        ::-webkit-scrollbar-thumb { background: var(--border); border-radius: 3px; }
        ::-webkit-scrollbar-thumb:hover { background: var(--neon); }
    </style>
</head>
<body>
    <div class="header">
        <div>
            <div class="logo-text">⚡ SIGNAL-X</div>
            <div class="logo-sub">OTOMATIK AG GUVENLIK DENETCISI v1.0.0</div>
        </div>
        <div class="header-right">
            <div class="theme-switcher">
                <span class="theme-label">TEMA:</span>
                <div class="theme-btn t-cyber active" onclick="setTheme('', this)" title="Cyberpunk"></div>
                <div class="theme-btn t-matrix" onclick="setTheme('theme-matrix', this)" title="Matrix"></div>
                <div class="theme-btn t-danger" onclick="setTheme('theme-danger', this)" title="Tehlike"></div>
                <div class="theme-btn t-blue" onclick="setTheme('theme-cyber', this)" title="Cyber"></div>
                <div class="theme-btn t-gold" onclick="setTheme('theme-gold', this)" title="Gold"></div>
            </div>
            <div class="status-pill">
                <div class="status-dot"></div>
                <div class="status-text">AKTIF</div>
            </div>
        </div>
    </div>

    <div class="container">
        <div class="page-title">// <span>AG GUVENLIK PANELI</span> — TAM ERISIM</div>
        <div class="tabs">
            <div class="tab active" onclick="switchTab('port', this)">[ PORT TARAMA ]</div>
            <div class="tab" onclick="switchTab('network', this)">[ AG KESFΙ ]</div>
            <div class="tab" onclick="switchTab('report', this)">[ RAPOR ]</div>
        </div>

        <!-- PORT TARAMA -->
        <div id="port" class="panel active">
            <div class="card">
                <div class="card-title">▸ PORT TARAMA MODULU</div>
                <div class="form-row">
                    <input type="text" id="target" placeholder="HEDEF_IP (192.168.1.1)" />
                    <input type="number" id="startPort" placeholder="BASLANGIC" value="1" style="max-width:130px" />
                    <input type="number" id="endPort" placeholder="BITIS" value="1024" style="max-width:130px" />
                    <button class="btn btn-primary" id="scanBtn" onclick="startScan()">▶ TARA</button>
                </div>
                <div class="progress-wrap" id="progressWrap">
                    <div class="progress-bar"><div class="progress-fill"></div></div>
                    <div class="progress-text" id="progressText">// HEDEF TARANΙYOR...</div>
                </div>
            </div>
            <div id="scanResults" style="display:none">
                <div class="scan-meta">
                    <div class="scan-meta-item">TARGET: <span id="metaTarget">-</span></div>
                    <div class="scan-meta-item">SURE: <span id="metaTime">-</span></div>
                    <div class="scan-meta-item">TARANAN: <span id="metaTotal">-</span> PORT</div>
                </div>
                <div class="stats-row">
                    <div class="stat-card">
                        <div class="stat-label">ISLETIM SISTEMI</div>
                        <div class="stat-value" id="statOS">-</div>
                    </div>
                    <div class="stat-card">
                        <div class="stat-label">GUVENLIK PUANI</div>
                        <div class="stat-value" id="statScore">-</div>
                    </div>
                    <div class="stat-card">
                        <div class="stat-label">ACIK PORT</div>
                        <div class="stat-value neon3" id="statOpen">-</div>
                    </div>
                    <div class="stat-card">
                        <div class="stat-label">YUKSEK RISK</div>
                        <div class="stat-value red" id="statRisk">-</div>
                    </div>
                </div>
                <div class="card">
                    <div class="card-title">▸ ACIK PORT LISTESI</div>
                    <div class="table-wrap">
                        <table>
                            <thead><tr><th>PORT</th><th>SERVIS</th><th>RISK</th><th>DURUM</th></tr></thead>
                            <tbody id="portTable"></tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>

        <!-- AG KESFI -->
        <div id="network" class="panel">
            <div class="card">
                <div class="card-title">▸ AG KESFI MODULU</div>
                <div class="form-row">
                    <input type="text" id="baseIP" placeholder="AG_ADRESI (192.168.1)" />
                    <input type="number" id="startIP" value="1" style="max-width:110px" />
                    <input type="number" id="endIP" value="254" style="max-width:110px" />
                    <button class="btn btn-primary" id="netBtn" onclick="startNetScan()">▶ TARA</button>
                </div>
            </div>
            <div id="netResults" style="display:none">
                <div class="stats-row">
                    <div class="stat-card">
                        <div class="stat-label">BULUNAN CIHAZ</div>
                        <div class="stat-value" id="statDevices">0</div>
                    </div>
                </div>
                <div class="card">
                    <div class="card-title">▸ AKTIF CIHAZLAR</div>
                    <div class="table-wrap">
                        <table>
                            <thead><tr><th>IP ADRESI</th><th>DURUM</th></tr></thead>
                            <tbody id="netTable"></tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>

        <!-- RAPOR -->
        <div id="report" class="panel">
            <div class="card">
                <div class="card-title">▸ GUVENLIK RAPORU</div>
                <pre id="reportContent">// HENUZ TARAMA YAPILMADI
// ONCE PORT TARAMA SEKMESINDEN BIR TARAMA BASLATIN</pre>
                <div class="report-actions">
                    <button class="btn btn-secondary" onclick="copyReport()">📋 KOPYALA</button>
                    <button class="btn btn-secondary" onclick="downloadReport()">⬇ MD INDIR</button>
                    <button class="btn btn-cyan" onclick="downloadJSON()">⬇ JSON INDIR</button>
                </div>
            </div>
        </div>
    </div>

    <script>
        const RISK_PORTS = [21, 23, 445, 3389, 6379];
        let lastScanData = null;
        let scanStartTime = null;

        function setTheme(theme, btn) {
            document.body.className = theme;
            document.querySelectorAll('.theme-btn').forEach(b => b.classList.remove('active'));
            btn.classList.add('active');
        }

        function switchTab(tab, el) {
            document.querySelectorAll('.tab').forEach(t => t.classList.remove('active'));
            document.querySelectorAll('.panel').forEach(p => p.classList.remove('active'));
            el.classList.add('active');
            document.getElementById(tab).classList.add('active');
        }

        function getRiskBadge(port) {
            if (RISK_PORTS.includes(port)) return '<span class="badge badge-risk">YUKSEK RISK</span>';
            if (port < 1024) return '<span class="badge badge-warn">ORTA RISK</span>';
            return '<span class="badge badge-low">DUSUK RISK</span>';
        }

        async function startScan() {
            const target = document.getElementById('target').value.trim();
            const startPort = parseInt(document.getElementById('startPort').value);
            const endPort = parseInt(document.getElementById('endPort').value);
            if (!target) { alert('IP adresi girin!'); return; }

            const btn = document.getElementById('scanBtn');
            btn.disabled = true;
            btn.textContent = '⏳ TARANIYOR...';
            document.getElementById('progressWrap').style.display = 'block';
            document.getElementById('scanResults').style.display = 'none';
            scanStartTime = Date.now();

            const textEl = document.getElementById('progressText');
            const interval = setInterval(() => {
                const elapsed = ((Date.now() - scanStartTime) / 1000).toFixed(1);
                textEl.textContent = `// HEDEF TARANΙYOR... ${elapsed}s`;
            }, 100);

            try {
                const res = await fetch('/api/scan', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ target, start_port: startPort, end_port: endPort })
                });
                const data = await res.json();
                lastScanData = data;
                clearInterval(interval);
                document.getElementById('progressWrap').style.display = 'none';
                showScanResults(data, startPort, endPort);
            } catch (e) {
                clearInterval(interval);
                alert('HATA: ' + e.message);
            } finally {
                btn.disabled = false;
                btn.textContent = '▶ TARA';
            }
        }

        function showScanResults(data, startPort, endPort) {
            const elapsed = ((Date.now() - scanStartTime) / 1000).toFixed(1);
            const riskCount = data.open_ports.filter(p => RISK_PORTS.includes(p.port)).length;

            document.getElementById('scanResults').style.display = 'block';
            document.getElementById('metaTarget').textContent = data.target;
            document.getElementById('metaTime').textContent = elapsed + 's';
            document.getElementById('metaTotal').textContent = endPort - startPort + 1;
            document.getElementById('statOS').textContent = data.os_guess;
            document.getElementById('statOpen').textContent = data.open_ports.length;
            document.getElementById('statRisk').textContent = riskCount;

            const score = data.security_score;
            document.getElementById('statScore').innerHTML =
                `<span class="score-badge score-${score}">${score}</span>`;

            const tbody = document.getElementById('portTable');
            tbody.innerHTML = '';
            if (data.open_ports.length === 0) {
                tbody.innerHTML = '<tr><td colspan="4" style="text-align:center;color:var(--dim);padding:30px;letter-spacing:2px">// ACIK PORT BULUNAMADI</td></tr>';
            } else {
                data.open_ports.forEach(p => {
                    tbody.innerHTML += `<tr>
                        <td><strong style="color:var(--neon)">${p.port}</strong></td>
                        <td>${p.service || '-'}</td>
                        <td>${getRiskBadge(p.port)}</td>
                        <td><span class="badge badge-open">ACIK</span></td>
                    </tr>`;
                });
            }
            document.getElementById('reportContent').textContent = data.report_md;
        }

        async function startNetScan() {
            const baseIP = document.getElementById('baseIP').value.trim();
            const start = parseInt(document.getElementById('startIP').value);
            const end = parseInt(document.getElementById('endIP').value);
            if (!baseIP) { alert('Ag adresi girin!'); return; }

            const btn = document.getElementById('netBtn');
            btn.disabled = true;
            btn.textContent = '⏳ TARANIYOR...';

            try {
                const res = await fetch('/api/network', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ base_ip: baseIP, start, end })
                });
                const data = await res.json();
                document.getElementById('netResults').style.display = 'block';
                document.getElementById('statDevices').textContent = data.devices.length;

                const tbody = document.getElementById('netTable');
                tbody.innerHTML = '';
                if (data.devices.length === 0) {
                    tbody.innerHTML = '<tr><td colspan="2" style="text-align:center;color:var(--dim);padding:30px;letter-spacing:2px">// AKTIF CIHAZ BULUNAMADI</td></tr>';
                } else {
                    data.devices.forEach(d => {
                        tbody.innerHTML += `<tr>
                            <td><strong style="color:var(--neon)">${d.ip}</strong></td>
                            <td><span class="badge badge-alive">AKTIF</span></td>
                        </tr>`;
                    });
                }
            } catch (e) {
                alert('HATA: ' + e.message);
            } finally {
                btn.disabled = false;
                btn.textContent = '▶ TARA';
            }
        }

        function copyReport() {
            navigator.clipboard.writeText(document.getElementById('reportContent').textContent);
            alert('RAPOR KOPYALANDI');
        }

        function downloadReport() {
            const text = document.getElementById('reportContent').textContent;
            const blob = new Blob([text], { type: 'text/markdown' });
            const a = document.createElement('a');
            a.href = URL.createObjectURL(blob);
            a.download = 'signal-x-rapor.md';
            a.click();
        }

        function downloadJSON() {
            if (!lastScanData) { alert('ONCE TARAMA YAPIN!'); return; }
            const blob = new Blob([JSON.stringify(lastScanData, null, 2)], { type: 'application/json' });
            const a = document.createElement('a');
            a.href = URL.createObjectURL(blob);
            a.download = 'signal-x-rapor.json';
            a.click();
        }
    </script>
</body>
</html>